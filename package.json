{
  "name": "mixrouter",
  "version": "3.0.0",
  "private": true,
  "description": "本地 Anthropic 协议模型路由器 + 会话级渠道分发(同一 Agent 多对话走不同 key)+ cc-switch 式客户端配置切换(零依赖,Node >= 18)",
  "main": "mixrouter.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node mixrouter.js",
    "test": "node --test test/*.test.js"
  },
  "license": "MIT"
}
